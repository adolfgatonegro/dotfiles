#! /bin/sh

nsxiv -t ~/documents/books/*

# booksdir="$HOME/books"
# booktitle=$(ls $booksdir | dmenu -F -i -p "Select a book:" -l 20)
# bookfile=$booksdir/$booktitle/$booktitle.pdf
#
# echo "$bookfile"
